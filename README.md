## executar o comando ' npm link ' dentro do diretorio do projeto

```
$ npm link
```

## para execução do projeto, rodar o comando ' create-node-service ' em qualquer diretorio diferente do atual

```
$ create-node-service
```
